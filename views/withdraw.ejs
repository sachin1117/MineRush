<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/public/css/withdraw.css">
  <title>Withdraw</title>
</head>

<body>
  <div>
    <h1>Withdraw Funds</h1>
    <form action="/withdraw" method="POST">
      <p>Win Balance: ₹ <%= win_balance %></p>
      <br>
      <label for="phone">Phone Number:</label>
      <input type="text" id="phone" name="phone" value="<%= phone_number %>" readonly>
      <br>
      <label for="amount">Amount:</label>
      <input type="number" id="amount" name="amount" required placeholder="Enter amount to withdraw" min="250">
      <small>Minimum withdrawal amount is ₹250.</small>
      <br>
      <label for="upi">UPI ID:</label>
      <input type="text" id="upi" name="upi" required placeholder="Enter UPI ID">
      <br>
      <button type="submit">Request Withdrawal</button>
    </form>

    <!-- Error handling for withdrawal amount less than ₹250 -->
    <% if (amount && amount < 250) { %>
      <p style="color: red;">withdraw amount must be at least ₹250.</p>
    <% } %>

    <% if (withdrawRequest) { %>
      <h2>Withdrawal Requested</h2>
      <p>Amount: ₹ <%= amount %></p>
      <p>Transaction ID: <%= transactionId %></p>
      <p>UPI ID: <%= upi %></p>
      <p>Your withdrawal request has been submitted and is under review.</p>
    <% } %>

    <a href="/"><button>Back to Home</button></a>
  </div>
</body>

</html>
